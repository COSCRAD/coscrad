/**
 * Note that the keys here are the extension we would associate with the
 * given MIMEType. This may break down at some point if the association is not
 * 1-to-1, though.
 */
export enum MIMEType {
    mp3 = 'audio/mpeg',
    mp4 = 'video/mp4',
    png = 'image/png',
}
