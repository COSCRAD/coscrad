import { Typography } from '@mui/material';

interface IndexMobileGridPresenterProps {}

export const IndexMobileGridPresenter = (): JSX.Element => {
    const foo = 'lol';

    return (
        <>
            <Typography variant="h2">Mobile Grid Index View</Typography>
        </>
    );
};
