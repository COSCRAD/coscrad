/**
 * **Do not** import these constants into production code.
 */

export const ERROR_TEST_ID = 'error';

// DO we want a TestId enum instead?
export const LOADING_TEST_ID = 'loading';

export const DEFAULT_ARTIFICIAL_DELAY = 150;
