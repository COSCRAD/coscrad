import { NotFoundPresenter } from '../not-found';

export const MemoryMatchIndexPage = (): JSX.Element => {
    const rounds = [];

    if (rounds.length === 0) {
        return <NotFoundPresenter />;
    }
};
