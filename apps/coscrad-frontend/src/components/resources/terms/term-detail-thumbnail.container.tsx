import { ResourceCompositeIdentifier } from '@coscrad/api-interfaces';

export const TermDetailThumbnailContainer = ({ id }: ResourceCompositeIdentifier): JSX.Element => {
    throw new Error('Not implemented...');
};
