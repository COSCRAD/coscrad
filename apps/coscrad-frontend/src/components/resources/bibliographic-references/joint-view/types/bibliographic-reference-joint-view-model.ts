/**
 * We sometimes want to consolidate the union type into a single view.
 */
export type BibliographicReferenceJointViewModel = {
    id: string;
    type: string;
    title: string;
    citation: string;
};
