/**
 * We decided against `CATEGORIES = 'categories'` because the plural would suggest
 * that the state has an array of categories, which it does not. It is a tree
 * structure.
 */
export const CATEGORY_TREE = 'categoryTree';
