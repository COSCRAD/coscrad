/**
 * Note that we are exporting this from the directory for this command
 * because we anticipate that `CREATE_TRANSCRIPT` will apply to a second
 * resource `Video` in the future.
 */
export const CREATE_TRANSCRIPT = 'CREATE_TRANSCRIPT';

export const ADD_PARTICIPANT_TO_TRANSCRIPT = `ADD_PARTICIPANT_TO_TRANSCRIPT`;

export const ADD_LINE_ITEM_TO_TRANSCRIPT = `ADD_LINE_ITEM_TO_TRANSCRIPT`;

export const IMPORT_LINE_ITEMS_TO_TRANSCRIPT = `IMPORT_LINE_ITEMS_TO_TRANSCRIPT`;

export const IMPORT_TRANSLATIONS_FOR_TRANSCRIPT = `IMPORT_TRANSLATIONS_FOR_TRANSCRIPT`;
